export const getMetaTitle = (title: string) => {
  return `${title} | Fabián Scherle`;
};

export const getMetaDescription = (description: string) => {
  return `${description} - Portfolio de Fabián Scherle, Desarrollador Full Stack y Arquitecto de Software`;
};